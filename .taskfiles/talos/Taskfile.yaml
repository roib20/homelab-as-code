---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: '3'

set: [pipefail]
shopt: [globstar]

tasks:
  cluster:
    desc: "Apply Talos cluster via Terragrunt (non-interactive)"
    dir: "{{.TERRAGRUNT_DIR}}/{{.TERRAGRUNT_ENV}}/pve/talos-cluster"
    interactive: false
    cmd: terragrunt stack run apply --non-interactive
    preconditions:
      - test -f "{{.TERRAGRUNT_DIR}}/{{.TERRAGRUNT_ENV}}/pve/talos-cluster/terragrunt.stack.hcl"
      - which terragrunt
      - which tofu
