---
apiVersion: v1
kind: Service
metadata:
  name: cnpg-bootstrap-webhook
  namespace: cnpg-bootstrap-system
spec:
  selector:
    app.kubernetes.io/name: cnpg-bootstrap-webhook
  ports:
    - name: https
      port: 443
      targetPort: https
