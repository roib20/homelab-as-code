---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: external-dns-target-ddns
  namespace: istio-ingress
rules:
  - apiGroups:
      - ""
    resources:
      - services
    resourceNames:
      - istio-ingressgateway
    verbs:
      - get
      - patch
