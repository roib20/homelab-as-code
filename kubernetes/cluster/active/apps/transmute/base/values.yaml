---
global:
  createDefaultServiceAccount: true
defaultPodOptions:
  automountServiceAccountToken: false
controllers:
  transmute:
    strategy: Recreate
    pod:
      hostUsers: false
      annotations:
        secret.reloader.stakater.com/reload: "transmute-kanidm-oauth2-credentials"
      securityContext:
        runAsNonRoot: true
        runAsGroup: 1000
        runAsUser: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
    containers:
      main:
        image:
          repository: ghcr.io/transmute-app/transmute
          tag: 1.3.1@sha256:61dbb43f6f29cd46d659534c0ec19faa4d0c50ea3a62630d313e8b7fef528bdb
        env:
          APP_URL: "https://convert.${FQDN_01}"
          DATA_DIR: /app/data
          HOME: /tmp
          OIDC_ISSUER_URL: "https://auth.${FQDN_01}/oauth2/openid/$(OIDC_CLIENT_ID)"
          OIDC_CLIENT_ID:
            valueFrom:
              secretKeyRef:
                name: transmute-kanidm-oauth2-credentials
                key: CLIENT_ID
          OIDC_CLIENT_SECRET:
            valueFrom:
              secretKeyRef:
                name: transmute-kanidm-oauth2-credentials
                key: CLIENT_SECRET
          OIDC_DISPLAY_NAME: Kanidm
          OIDC_AUTO_CREATE_USERS: "true"
          OIDC_AUTO_LAUNCH: "true"
          ALLOW_UNAUTHENTICATED: "false"
        resources:
          requests:
            cpu: 100m
            memory: 512Mi
          limits:
            memory: 2Gi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
        probes:
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /api/health/live
                port: &port 3313
              periodSeconds: 30
              timeoutSeconds: 10
              failureThreshold: 3
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /api/health/ready
                port: *port
              periodSeconds: 30
              timeoutSeconds: 10
              failureThreshold: 3
          startup:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /api/health/ready
                port: *port
              periodSeconds: 10
              timeoutSeconds: 10
              failureThreshold: 30
service:
  main:
    controller: transmute
    ports:
      http:
        port: *port
persistence:
  data:
    accessMode: ReadWriteOnce
    size: 10Gi
    storageClass: longhorn
    globalMounts:
      - path: /app/data
  tmpfs:
    type: emptyDir
    globalMounts:
      - path: /tmp
