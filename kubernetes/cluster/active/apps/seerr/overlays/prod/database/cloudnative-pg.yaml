---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
    postgresql.cnpg.homelab.towerofkubes.com/barman-server-name: seerr-postgres
  name: seerr-postgres
spec:
  instances: 2
  affinity:
    enablePodAntiAffinity: true
    topologyKey: kubernetes.io/hostname
    podAntiAffinityType: required
  managed:
    services:
      disabledDefaultServices:
        - ro
        - r
  storage:
    size: 5Gi
    storageClass: longhorn-cnpg
  resources:
    requests:
      cpu: 60m
      memory: 128Mi
    limits:
      memory: 512Mi
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: cnpg-backups
  bootstrap:
    initdb:
      database: seerr
      owner: seerr
