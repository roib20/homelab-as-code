---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: opengist
  namespace: opengist
spec:
  template:
    spec:
      containers:
        - name: opengist
          command:
            - /app/opengist/opengist
            - --config
            - /config.yml
