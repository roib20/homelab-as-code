---
persistence:
  downloads:
    existingClaim: smb-mediadata-rwx
    advancedMounts:
      pinepods:
        app:
          - path: /opt/pinepods/downloads
            subPath: Media/Audio/Podcasts/pinepods
            readOnly: false

rawResources:
  smb-mediadata-rwx:
    forceRename: smb-mediadata-rwx
    manifest:
      apiVersion: v1
      kind: PersistentVolumeClaim
      spec:
        accessModes:
          - ReadWriteMany
        storageClassName: smb-mediadata
        resources:
          requests:
            storage: 100Ti

  httproute:
    forceRename: pinepods
    manifest:
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      metadata:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/description: Self-hosted podcasts server.
          gethomepage.dev/group: Media
          gethomepage.dev/icon: sh-pinepods.svg
          gethomepage.dev/name: pinepods
          gethomepage.dev/pod-selector: "app.kubernetes.io/instance=pinepods"
      spec:
        parentRefs:
          - group: gateway.networking.k8s.io
            kind: Gateway
            name: http-gateway-api
            namespace: envoy-gateway-ingress
        hostnames:
          - "podcasts.${FQDN_01}"
        rules:
          - backendRefs:
              - group: ""
                kind: Service
                name: pinepods-app
                namespace: pinepods
                port: 8040
                weight: 1
            matches:
              - path:
                  type: PathPrefix
                  value: /

  httproute-redirect:
    forceRename: pinepods-redirect
    manifest:
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      spec:
        parentRefs:
          - group: gateway.networking.k8s.io
            kind: Gateway
            name: http-gateway-api
            namespace: envoy-gateway-ingress
        hostnames:
          - "pinepods.${FQDN_01}"
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            filters:
              - type: RequestRedirect
                requestRedirect:
                  hostname: "podcasts.${FQDN_01}"
                  scheme: https
                  statusCode: 301
