---
global:
  createDefaultServiceAccount: true
controllers:
  main:
    strategy: RollingUpdate
    containers:
      app:
        image:
          repository: ghcr.io/alam00000/bentopdf-simple
          tag: 2.8.5@sha256:268f3e4a1aeed5d7035b58ab1d679da85b7c559ac3d3c08eb2177520c3647e1f
        env:
          TZ: Asia/Jerusalem
        probes:
          startup:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /
                port: &port 8080
              periodSeconds: 5
              timeoutSeconds: 3
              failureThreshold: 30
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /
                port: *port
              periodSeconds: 10
              timeoutSeconds: 3
              failureThreshold: 3
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /
                port: *port
              periodSeconds: 30
              timeoutSeconds: 3
              failureThreshold: 3
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
              - ALL
        resources:
          requests:
            cpu: 10m
          limits:
            memory: 256Mi
defaultPodOptions:
  automountServiceAccountToken: false
  enableServiceLinks: false
  securityContext:
    runAsNonRoot: true
    runAsUser: 101
    runAsGroup: 101
    fsGroup: 101
    fsGroupChangePolicy: OnRootMismatch
    seccompProfile:
      type: RuntimeDefault
service:
  app:
    ports:
      http:
        targetPort: *port
        port: *port
persistence:
  tmp:
    type: emptyDir
    medium: Memory
    sizeLimit: 32Mi
    globalMounts:
      - subPath: cache
        path: /var/cache/nginx
      - subPath: etc-tmp
        path: /etc/nginx/tmp
      - subPath: var-tmp
        path: /var/lib/nginx/tmp
