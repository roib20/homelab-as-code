---
- when: ansible_facts["os_family"] == "Debian"
  block:
    - ansible.builtin.include_tasks:
        file: deb/apt-autoremove.yml

- when: ansible_facts["os_family"] == "RedHat"
  block:
    - ansible.builtin.include_tasks:
        file: rpm/dnf-autoremove.yml
